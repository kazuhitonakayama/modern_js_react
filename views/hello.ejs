<script>
  // アロー関数
  // const returnName = (value) => {
  //   return 'kazu';
  // };
  const returnName = value => {
    return 'kazu';
  };

  // アロー関数の引数が1つのとき、`()`を省略できる
  console.log(returnName('kazu'));
</script>

<h1>Hello World</h1>
